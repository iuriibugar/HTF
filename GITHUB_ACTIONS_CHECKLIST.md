# Перевірка GitHub Actions для деплою

## ✅ Що я створив:
1. **`.github/workflows/deploy.yml`** - GitHub Actions workflow для автоматичного деплою

## ��� Що потрібно перевірити на GitHub:

### 1. GitHub Pages Налаштування
Перейдіть в: **Settings → Pages**
- ✅ Source повинен бути: **GitHub Actions**
- ✅ Branch **main** або **master** (на якій ви робите push)

### 2. Перевірити Actions
Перейдіть в: **Actions**
- ✅ Повинен бути workflow **"Deploy to GitHub Pages"**
- ✅ Перевірьте статус останніх runs
- Якщо червоний ❌ - натисніть на нього і подивіться логи помилок

### 3. Перевірити що push підпав в правильну branch
```bash
git branch -a  # показує всі branches
git log --oneline | head -5  # показує останні commits
```

### 4. Якщо workflow fails - перевірте:
- ✅ Чи `npm install` проходить без помилок локально?
- ✅ Чи `npm run build` успішно будує проект?
- ✅ Чи всі залежності вказані в `package.json`?

## ��� Як запустити локально:
```bash
npm run build  # будує проект
npm run preview  # перевіряє build
npm run deploy  # деплоїть на GitHub Pages
```

## ��� Якщо workflow все ще fails:
1. Перевірте **Actions → найновіший run** → дивіться Build step
2. Копіюйте повідомлення про помилку
3. Найчастіші проблеми:
   - Забули додати `.github/workflows/deploy.yml` в git ✅ (зроблено)
   - Неправильна branch в workflow ✅ (встановлена main/master)
   - Залежності не встановлені - перевірте `package-lock.json`
